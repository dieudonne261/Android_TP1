<!-- shadow_background.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background with rounded corners and stroke -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" /> <!-- Background color -->
            <corners android:radius="8dp" /> <!-- Corner radius -->
            <stroke
                android:width="1dp"
                android:color="#DDDDDD" /> <!-- Border color and width -->
        </shape>
    </item>

    <!-- Shadow layer -->
    <item android:right="1dp" android:left="1dp" android:bottom="2dp" android:top="1dp">
        <shape android:shape="rectangle">
            <solid android:color="#80000000" /> <!-- Shadow color -->
            <corners android:radius="8dp" /> <!-- Same corner radius as background -->
        </shape>
    </item>

</layer-list>
